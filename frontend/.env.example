# Local development — copy to .env.local and fill in your Render backend URL.
# On Vercel, set NEXT_PUBLIC_API_URL in Project Settings → Environment Variables.
NEXT_PUBLIC_API_URL=http://localhost:4000/api
